name: Build C++ project

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v2
    - name: Build with MSBuild
      run: msbuild MyDLL.sln /p:Configuration=Release
